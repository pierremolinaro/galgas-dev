{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {checkForNewVersion = id; resetLIPMpathAction = id; setLIPMpathAction = id; }; 
            CLASS = GGSUpdateCocoaGalgas; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                mCancelButton = NSButton; 
                mCheckNowButton = NSButton; 
                mCheckUpdateAtStartUpCheckBox = NSButton; 
                mDownloadProgressIndicator = NSProgressIndicator; 
                mDownloadSubTitle = NSTextField; 
                mDownloadTitle = NSTextField; 
                mLIBPMStatusTextField = NSTextField; 
                mLIBPMpathTextField = NSTextField; 
                mUpdateView = NSView; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}