lexique dangling_else_solution_lexique {

#--- Key words
list keyWordList %errorMessage "the '%K' key word"{
 "I", # IF EXPR THEN
 "E", # ELSE
 "O" } # OTHER
rule list keyWordList

#--- Identifiers
$id$ %errorMessage "an identifier"

@string identifierString

rule 'a'->'z' | 'A'->'Z' {
  enterCharacterIntoString (!?identifierString !toLower (!*) )
  send %codeFor identifierString in keyWordList default $id$
}

#--- Delimitors
list delimitorsList %errorMessage "the '%K' delimitor"{
  "+" , "*", "(", ")" }
rule list delimitorsList

# --------------------- separators -----------------------------------------
rule '\u0001' -> ' ' {
}

}
